spring:
  application:
    name: mywork-be
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  datasource:
    url: jdbc:mysql://localhost:3306/mywork_db
    username: root
    password: mywork2503
    driver-class-name: com.mysql.cj.jdbc.Driver
